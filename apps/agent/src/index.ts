/**
 * @cfls/agent — the per-user CoordinationAgent: one outbound WSS connection,
 * loopback-only Local_API, embedded MCP server, folder watcher, encrypted cache,
 * reconnect/re-assert, and Windows packaging.
 *
 * Placeholder only — implemented in spec tasks 9.1–9.9.
 */

export const APP_NAME = "@cfls/agent";

export {};
