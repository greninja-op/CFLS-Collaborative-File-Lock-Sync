/**
 * @cfls/vscode-extension — the VS Code Editor_Extension. Talks only to the local
 * CoordinationAgent via the Local_API, emits editor events, renders coordination
 * state, and enforces hard-stop for cooperating edits.
 *
 * Placeholder only — implemented in spec tasks 11.1–11.5.
 */

export const APP_NAME = "@cfls/vscode-extension";

export {};
