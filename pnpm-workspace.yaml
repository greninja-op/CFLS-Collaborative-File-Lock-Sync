packages:
  - "apps/*"
  - "packages/*"

# Allow esbuild (used by tsup) to run its post-install so it can fetch its
# platform binary. Without this, pnpm blocks the build script.
allowBuilds:
  esbuild: true

onlyBuiltDependencies:
  - esbuild
